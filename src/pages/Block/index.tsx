import { createSignal, Component } from "solid-js";

const Block: Component = () => {
  return (
    <div style={{ padding: "20px", "font-family": "Arial, sans-serif" }}>
      <h1 style={{ "text-align": "center" }}>Block</h1>
    </div>
  );
};

export default Block;
